{
  "name": "zig-auth",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "zig build -Doptimize=ReleaseSmall && cp zig-out/bin/zig-auth.wasm worker/",
    "dev": "pnpm run build && pnpm exec wrangler dev",
    "deploy": "pnpm run build && pnpm exec wrangler deploy",
    "test:zig": "zig build test"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
